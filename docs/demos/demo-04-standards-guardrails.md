# Demo 04: Standards and guardrails

## Goal

Show that the workflow is repeatable because standards are explicit and reusable.

## Walkthrough

- Open `CONTRIBUTING.md` and the standards docs under `/docs/standards/`.
- Show `.github/ISSUE_TEMPLATE/` and `.github/PULL_REQUEST_TEMPLATE.md`.
- Explain how `.github/workflows/ci.yml` delegates to `reusable-quality-gates.yml`.
- Point to `.github/CODEOWNERS` and describe required-review routing.
- Close with the definition of done and why the process never auto-merges changes blindly.
