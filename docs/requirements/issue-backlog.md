# Workshop Issue Backlog (Sample)

1. **Ingestion Endpoint** - Implement `POST /events` validation and accepted response.
2. **Rules Engine** - Add deterministic `temperature > threshold` rule evaluation.
3. **Work Order Persistence** - Create in-memory work-order records with source event linkage.
4. **Audit Logging** - Append structured audit entries with correlation IDs.
5. **Notification Stub** - Emit non-blocking notification placeholder on work-order creation.
6. **CI + Standards** - Add reusable workflow and quality gate checks (format/lint/tests).
7. **Observability Enhancements** - Ensure required audit fields and traceability across actions.
8. **ADR Follow-up** - Open ADR request issue for schema evolution path to `/v2/events`.
